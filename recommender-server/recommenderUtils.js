const defaultRec = {
    dishes: [
        {
            id: '629202c76a579f0257f7aadb',
            match: 0.92,
            users: [1, 3],
        },
    ],
};

const calculateScores = (users, dishes, tags) => {
    return defaultRec; // todo
};

module.exports = {
    calculateScores,
};
